function divisibleCount(x, y, k) {
    //code me
    function divisibleCount(x, y, k) {
        //code me
       
       // return array of nums from x to y
       
       //if (arr[i]%k === 0) {counter++}
       
     }
     
 }
 