function sumDigNthTerm(initval, patternl, nthterm) {
    // your code
  
  let val = initval 
  
  //create a for loop that runs until the incrementer is equal to the length of nth term
  
  //stringify the value at nth term in order to return the sum
  
}